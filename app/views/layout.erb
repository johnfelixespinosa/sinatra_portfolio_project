<!doctype html>
<html>
  <head>
    <title>Schedule Maker</title>
  </head>
  <body>
    <center>

      <% if logged_in? %>

        <a href="/schedule/new">Create New Schedule</a><br>
        <%= yield %>
        <a href="/logout">Logout</a>

      <% else %>

        <a href="/signup">Signup</a><br>
        <a href="/login">Login</a>
        <%= yield %>

      <% end %> 
    
      <!-- <%= yield %>  -->
      
      
    </center>
  </body>
</html>
